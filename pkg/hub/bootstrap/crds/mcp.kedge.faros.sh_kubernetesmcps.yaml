---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.16.5
  name: kubernetesmcps.mcp.kedge.faros.sh
spec:
  group: mcp.kedge.faros.sh
  names:
    kind: KubernetesMCP
    listKind: KubernetesMCPList
    plural: kubernetesmcps
    singular: kubernetesmcp
    shortNames:
    - kmcp
  scope: Cluster
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.endpoint
      name: Endpoint
      type: string
    - jsonPath: .status.connectedEdges
      name: ConnectedEdges
      type: integer
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: KubernetesMCP represents a multi-edge MCP server for a set of
          Kubernetes edges matched by a label selector.
        properties:
          apiVersion:
            description: APIVersion defines the versioned schema of this representation
              of an object.
            type: string
          kind:
            description: Kind is a string value representing the REST resource this
              object represents.
            type: string
          metadata:
            type: object
          spec:
            description: KubernetesMCPSpec defines the desired state of KubernetesMCP.
            properties:
              edgeSelector:
                description: EdgeSelector selects which edges to include in this MCP
                  server. An empty selector matches all connected edges.
                properties:
                  matchExpressions:
                    description: matchExpressions is a list of label selector requirements.
                    items:
                      description: A label selector requirement is a selector that
                        contains values, a key, and an operator that relates the key
                        and values.
                      properties:
                        key:
                          description: key is the label key that the selector applies
                            to.
                          type: string
                        operator:
                          description: operator represents a key's relationship to
                            a set of values.
                          type: string
                        values:
                          description: values is an array of string values.
                          items:
                            type: string
                          type: array
                          x-kubernetes-list-type: atomic
                      required:
                      - key
                      - operator
                      type: object
                    type: array
                    x-kubernetes-list-type: atomic
                  matchLabels:
                    additionalProperties:
                      type: string
                    description: matchLabels is a map of key-value pairs.
                    type: object
                type: object
                x-kubernetes-map-type: atomic
              readOnly:
                description: ReadOnly makes all MCP tools read-only when true.
                type: boolean
              toolsets:
                description: Toolsets defines which kubernetes-mcp-server toolsets
                  to enable. Defaults to all toolsets if empty.
                items:
                  type: string
                type: array
            type: object
          status:
            description: KubernetesMCPStatus defines the observed state of KubernetesMCP.
            properties:
              conditions:
                description: Conditions describe the current state of the KubernetesMCP.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: lastTransitionTime is the last time the condition
                        transitioned from one status to another.
                      format: date-time
                      type: string
                    message:
                      description: message is a human readable message indicating
                        details about the transition.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: observedGeneration represents the .metadata.generation
                        that the condition was set based upon.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: reason contains a programmatic identifier indicating
                        the reason for the condition's last transition.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              connectedEdges:
                description: ConnectedEdges is the number of currently connected edges
                  matching the selector.
                type: integer
              endpoint:
                description: Endpoint is the URL at which this MCP server is reachable.
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
