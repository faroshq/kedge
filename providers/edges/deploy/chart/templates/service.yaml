apiVersion: v1
kind: Service
metadata:
  name: {{ include "edges.fullname" . }}
  labels:
    {{- include "edges.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
  selector:
    {{- include "edges.selectorLabels" . | nindent 4 }}
