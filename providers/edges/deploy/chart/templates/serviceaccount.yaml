{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "edges.serviceAccountName" . }}
  labels:
    {{- include "edges.labels" . | nindent 4 }}
{{- end }}
