{
  "name": "kedge-provider-kubernetes-edges-portal",
  "version": "0.1.0",
  "private": true,
  "description": "Kubernetes-edges provider's portal-side micro-frontend. Vite builds it into a single main.js custom-element bundle served by the hub under /ui/providers/kubernetes-edges/ from an embedded fs.FS (see ../assets.go).",
  "type": "module",
  "scripts": {
    "build": "vite build",
    "dev": "vite",
    "typecheck": "vue-tsc --noEmit"
  }
}
