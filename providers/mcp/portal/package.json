{
  "name": "kedge-provider-mcp-portal",
  "version": "0.1.0",
  "private": true,
  "description": "MCP provider's portal-side micro-frontend. Vite builds it into a single main.js custom-element bundle that the kedge hub serves under /ui/providers/mcp/ from an embedded fs.FS (see ../assets.go).",
  "type": "module",
  "scripts": {
    "build": "vite build",
    "dev": "vite",
    "typecheck": "vue-tsc --noEmit"
  }
}
